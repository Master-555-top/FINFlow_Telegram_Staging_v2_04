# Privacy note for the shareable package

Архив `FINFlow_v3_ecosystem_v1_73.zip` подготовлен для безопасной передачи в GPT-чат и не содержит:

- `private_raw_data/source_intake`;
- банковских PDF и исходных CSV;
- нормализованных строк доходов/расходов/транзакций;
- извлечённого текста исходного чата;
- `.env.local`, токенов и service-role ключей;
- `node_modules`, `.next` и runtime-логов.

Спецификации, архитектура, исходный код, SQL migration, шаблон `.env.example`, changelog и state/handoff-документы сохранены.

Приватные исходники остаются только в локальной рабочей копии владельца. Не добавляйте их в публичный или внешний чат без отдельного осознанного решения.
