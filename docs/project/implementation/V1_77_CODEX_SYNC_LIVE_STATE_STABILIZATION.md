# v1.77 — Codex Sync + Live State Stabilization

## Added / changed

- accepted Codex `next.config.js` root fix;
- accepted Codex `package.json` scripts and pinned dependencies;
- accepted Codex live `DashboardShell`;
- accepted Codex live `DayCoreDashboard`;
- accepted Codex live `NetCalculationPanel`;
- patched current `DailyQuickInputPanel` with Codex hydration and parent-state sync fixes;
- preserved v1.73–v1.76 assistant/car/fuel/repair features.

## Why not copy Codex package completely

Codex package was corrected from v1.72. The active project had already advanced to v1.76. A full replacement would delete v1.73–v1.76 features, so v1.77 applies only the stabilization fixes while preserving current product layers.
