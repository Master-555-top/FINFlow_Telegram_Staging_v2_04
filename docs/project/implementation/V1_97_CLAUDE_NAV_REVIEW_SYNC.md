# v1.97 — Claude Nav Review Sync

## Added / merged

- Functional `NAV_TABS` in `DashboardShell`.
- Bottom navigation now switches between daily/money/system groups.
- System/deployment/audit panels no longer sit in the default daily flow.
- Old root manifests remain archived under `docs/project/manifests/archive`.
- Full Claude nav-review package preserved in private vault.

## UX impact

The daily screen is less overloaded: system/deployment checks are under `Система`, money/review/backup tools are under `Деньги`, and the default day flow focuses on day core + quick input.
