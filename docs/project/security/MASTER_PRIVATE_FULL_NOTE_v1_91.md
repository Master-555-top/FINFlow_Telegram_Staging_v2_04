# Master Private Full Note — v1.91

This working app is part of a larger MASTER PRIVATE FULL package.

The full package keeps private ecosystem context under:

```text
private_vault/
```

The working app must not import or bundle private_vault files into client/runtime code.
