# Security Scan — v1.49

No secrets added.

No real Supabase URL, anon key, service_role key, Telegram bot token or OpenAI key added.

This package adds only:
- architecture docs
- TypeScript persistence contract
- local adapter placeholder
- Supabase integration status/warnings

Critical rule:
- service_role key must never be bundled into frontend code.
