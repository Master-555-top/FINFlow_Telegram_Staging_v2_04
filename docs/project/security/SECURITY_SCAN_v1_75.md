# Security Scan — v1.75

No secrets added.

Repair fund allocation uses local day input and project car context only.

No env values, tokens, Supabase writes, OpenAI calls, n8n calls, raw bank files or private_raw_data are exposed.
