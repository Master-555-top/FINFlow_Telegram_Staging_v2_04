# Security Scan — v1.89

No secrets added.

Restore preview compares safe structural/day-state fields and does not expose tokens, `.env.local`, service-role keys, raw bank PDFs/CSVs or private_raw_data.

Restore remains local-only and does not write to Supabase.
