# Security Scan — v1.94

No secrets added.

Cloud apply rollback stores the local day document before applying loaded cloud preview. It is session-scoped and local-only.

No automatic Supabase writes were added.
