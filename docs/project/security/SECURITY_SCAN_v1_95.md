# Security Scan — v1.95

No secrets added.

Cloud save preflight reads only local backup metadata and rollback presence. It does not expose tokens or server secrets.

No automatic Supabase writes were added.
