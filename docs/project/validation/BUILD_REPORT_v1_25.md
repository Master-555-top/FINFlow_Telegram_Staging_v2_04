# Build Report — v1.25

Command:

```bash
npm install --ignore-scripts
NEXT_TELEMETRY_DISABLED=1 npx next build
```

Result:

```text
Compiled successfully
TypeScript finished
Static pages generated
```

Status: passed.

Note: first build command timed out after dependencies were installed and build was still running. Re-run completed successfully.
